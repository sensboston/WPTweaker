<UserControl
    x:Class="WPTweaker.ToggleTweak"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WPTweaker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tk="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    mc:Ignorable="d"
    Margin="0,10"
    d:DesignHeight="150"
    d:DesignWidth="480">

    <StackPanel>
        <Border Background="{StaticResource PhoneChromeBrush}" Padding="8">
            <TextBlock Text="{Binding Name}" FontSize="24" VerticalAlignment="Center" />
        </Border>
        <TextBlock Text="{Binding Description}" FontSize="18" Padding="8" TextWrapping="Wrap" />
        <tk:ToggleSwitch x:Name="Toggle" IsChecked="{Binding Value, Mode=TwoWay}" Margin="4,-20,4,0" />
    </StackPanel>
</UserControl>
